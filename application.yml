spring:
 application:
  name: DEfault
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8040/eureka
  instance:
    prefer-ip-address: true
